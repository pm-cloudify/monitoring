# Monitoring

here I have configured Prometheus and Grafana to monitor my node.

## Prometheus

Using prometheus to monitor my services stat and system metrics.
I have installed Node Exported to read node metric (e.g. CPU, Mem, ...).
